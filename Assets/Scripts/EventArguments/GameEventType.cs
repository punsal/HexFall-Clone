namespace EventArguments
{
    public enum GameEventType
    {
        CheckNeighbors,
        CheckPossibleMoves,
        MovePosition,
        UpdateMoveCount,
        UnselectAll,
        LevelComplete,
        NextState,
        GameOver
    }
}
